# Copy to .env and fill in your values.
# Neon: https://neon.tech — use the pooled connection string (Recommended for most uses).

DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require

# Optional: Vercel Postgres / Neon often expose these; the app uses them as fallback for DATABASE_URL.
# POSTGRES_URL=...
# POSTGRES_PRISMA_URL=...

# Optional: for one-command Netlify deploy (npm run netlify:deploy:ci)
# NETLIFY_AUTH_TOKEN=...   (User settings → Applications → New access token)
# NETLIFY_SITE_ID=...      (Site → Site settings → General → Site information)

# Auth: JWT secret (change in production)
JWT_SECRET=your-super-secret-key-change-in-production

# Optional
VITE_PUBLIC_BUILDER_KEY=
VITE_WHATSAPP_NUMBER=201061887799
PING_MESSAGE=ping pong
